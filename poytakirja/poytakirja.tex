\documentclass[11pt, a4paper]{article}

% Language and font encodings
\usepackage[utf8]{inputenc}
\usepackage[T1]{fontenc}
\usepackage[finnish]{babel}
\usepackage{lmodern}

% Margins and layout
\usepackage[margin=2.5cm]{geometry}
\usepackage{parskip} % Replaces paragraph indentation with vertical spacing
\usepackage{microtype}
\usepackage{lastpage} % Required for 'Page x of y' numbering

% Header and footer configuration
\usepackage{fancyhdr}
\pagestyle{fancy}
\fancyhf{}
\fancyhead[L]{\textbf{Tampere Hacklab ry}}
% The global header uses "Tyyppi --- Nimi" (e.g., Pöytäkirja --- Hallituksen kokous)
\fancyhead[C]{$if(tyyppi)$$tyyppi$$else$Pöytäkirja$endif$ --- $if(nimi)$$nimi$$else$Kokous$endif$}
\fancyhead[R]{$päivä$}
% Footer now displays x/y format
\fancyfoot[C]{Sivu \thepage/\pageref{LastPage}}
\renewcommand{\headrulewidth}{0.4pt}

% Packages required by Pandoc for formatting tables, images, and links
\usepackage{longtable,booktabs,array}
\usepackage{graphicx}
\usepackage{hyperref}
\hypersetup{
    colorlinks=true,
    linkcolor=black,
    urlcolor=blue,
    pdfborder={0 0 0}
}

% Pandoc requires this macro for its Markdown lists
\providecommand{\tightlist}{%
  \setlength{\itemsep}{0pt}\setlength{\parskip}{0pt}}

\begin{document}

% --- FIRST PAGE HEADER SECTION ---
\noindent
\textbf{Tampere Hacklab ry} \\
Ahlmanintie 56, 33800 Tampere \\
Y-tunnus: 2419748-5

\vspace{0.5cm}

\begin{center}
    {\LARGE \textbf{$if(nimi)$$nimi$$else$Kokous$endif$}}
\end{center}
\vspace{0.5cm}

% --- MEETING DETAILS ---
\noindent
\begin{tabular}{@{}p{3cm} p{12cm}}
\textbf{Aika:} & $päivä$ klo $aloitus$ \\
\textbf{Paikka:} & $paikka$ \\
\textbf{Läsnä:} & $for(paikalla)$$paikalla$$sep$, $endfor$ \\
$if(poissa)$
\textbf{Poissa:} & $for(poissa)$$poissa$$sep$, $endfor$ \\
$endif$
\end{tabular}

\vspace{0.5cm}

$if(asialista)$
\noindent\textbf{Asialista:}
\begin{enumerate}
$for(asialista)$
  \item $asialista$
$endfor$
\end{enumerate}
\vspace{0.5cm}
$endif$

\hrule
\vspace{0.5cm}

% --- MAIN CONTENT (Markdown text is injected here) ---
$body$

% --- END MATTER ---
$if(lopetus)$
\vspace{1cm}
\begin{samepage} % Ensures the heading and time stay on the same page
\noindent \textbf{Kokouksen päättäminen:} \\
Kokous päätettiin klo $lopetus$.
\end{samepage}
$endif$

% --- SIGNATURES ---
$if(tarkastajat)$
\vspace{1cm}
\begin{samepage} % Ensures the text and signatures stay on the same page
\noindent\textbf{Pöytäkirjan vakuudeksi:}

\vspace{0.5cm}
\noindent
$for(tarkastajat)$
\begin{minipage}[t]{7cm}
\vspace{1.5cm}
\hrule
\vspace{0.2cm}
$tarkastajat$\\
Pöytäkirjantarkastaja
\end{minipage}
\hspace{1cm}
$endfor$
\end{samepage}
$endif$

\end{document}
